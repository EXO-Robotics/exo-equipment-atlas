{
  "name": "exo-equipment-atlas",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "description": "Evidence-bound, manufacturer-neutral interactive equipment explorer platform",
  "scripts": {
    "build:site": "node scripts/build-site.mjs",
    "check": "node scripts/validate-repository.mjs",
    "check:production": "node scripts/validate-production-assets.mjs",
    "check:site": "node --check assets/site/app.js && node scripts/validate-site.mjs && node scripts/build-site.mjs",
    "check:sources": "node scripts/validate-repository.mjs --require-private-sources",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
